#include <iostream>

using namespace std;

int main() {
    int batteryPercent = 0;
    int motorCount = 0;

    cout << "Enter battery percent: ";
    cin >> batteryPercent;

    cout << "Enter motor count: ";
    cin >> motorCount;

    // TODO: If the battery is 50 or higher AND the motor count is 4 or higher,
    // print "Robot is ready for practice."
    // Otherwise, print "Robot needs a pit check."

    return 0;
}
